import React from "react";

import { Quiz } from "../Quiz";

import { HomeContainer } from "./styledComponents";

const Home = () => {
   return (
      <HomeContainer>
         <Quiz />
      </HomeContainer>
   );
};

export default Home;
